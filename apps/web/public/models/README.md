Prado on the map uses the PNG poses in `/sary/`:

- `prado-stationary-idle.png` — parked / paused / weak GPS
- `prado-moving-active.png` — recording and actually moving
- `prado-map-marker-64.png` — far zoom

Optional later: drop a licensed `prado.glb` here to replace the PNG poses.
